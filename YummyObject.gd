extends RigidBody2D
#@export var SpriteTexture = "res://SandwichItems/Tomato/Tomato.png"

# Called when the node enters the scene tree for the first time.
func _ready():
	#get_node("Sprite2D").texture = ResourceLoader.load(SpriteTexture)
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
